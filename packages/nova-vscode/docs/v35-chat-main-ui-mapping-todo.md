# V35 主界面功能映射 TODO

- [x] 模式选择补齐 `agent_team`（前端模式下拉可见、可选择）
- [x] 设置页补充 `Agent Team` 快捷配置入口（智能体行为页可直接启停并跳转高级配置）
- [x] 对话输入区增加代码库索引快捷入口（数据库图标，打开独立弹窗，不打断对话工作流）
- [x] 对话输入区增加“添加上下文(@)”快捷入口（直接触发文件/目录/agent/special 上下文提及）
- [x] 对话输入区增加附件按钮（支持图片与通用文件，纳入发送链路）
- [x] 右下角增加“智能体行为”快捷入口（打开独立弹窗，不跳转整页设置）
- [x] 扩展侧补齐 `requestCodebaseIndexStatus/reindexCodebase` 消息处理，避免按钮无响应
- [x] 增加 `clearCodebaseIndex` 消息处理，支持独立弹窗中的“清除索引”
- [x] 设置页新增顶部“搜索设置 + 保存”交互
- [x] 设置改为“草稿保存/撤销”模式（一级页与二级页独立生效，避免实时写入）
- [x] 打包验证 VSIX（版本 `7.0.35`）
